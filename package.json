{
  "name": "syr-ev-charger-map",
  "version": "3.1.0",
  "private": true,
  "description": "Syrian Renewables EV charger map with a protected Firebase administration layer.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check api/chargers.js && node --check api/firebase-config.js && node --check api/_lib/seed.js && node --check api/admin/chargers.js && node --check api/admin/resolve-maps.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
