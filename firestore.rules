rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All database access is performed by trusted Vercel server functions using
    // the Firebase Admin SDK. Browser clients receive no direct Firestore access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
